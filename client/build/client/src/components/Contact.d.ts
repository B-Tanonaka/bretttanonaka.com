import React, { Dispatch, SetStateAction } from 'react';
export default function Contact({ setModalOpen, setModalState, }: {
    setModalOpen: Dispatch<SetStateAction<any>>;
    setModalState: Dispatch<SetStateAction<any>>;
}): React.JSX.Element;
